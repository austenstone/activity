name: Usage
on:
  workflow_dispatch:

# Disabled push/PR triggers 2026-06-27: TOKEN PAT secret is expired.
# Re-enable after rotating TOKEN in repo Secrets. See github/austenstone-notes#456.

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./
        with:
          github-token: ${{ secrets.TOKEN }}
          organization: ${{ vars.ORGANIZATION }}
